def power(a, b):
    if b == 0:
        return 1
    elif b < 0:
        # Handles negative powers: a^-b = 1 / a^b
        return 1 / power(a, -b)
    else:
        return a * power(a, b - 1)
a=int(input())
b=int(input())
print(power(a,b))
